# Expense-tracker
Expense-tracker private repo
gui branch

WARNING:
Do NOT open the qt files in the same directory as the git cloned repo but move the qt file to a separate non-git folder and do your work then copy it back here in the git folder and then push it but Do NOT directly open Qt project files here
